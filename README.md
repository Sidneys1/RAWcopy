RAWcopy
===

A simple utility for copying the raw data of one file to another.

Usage:

* Enter the file path of the source file.
* Enter the number of bytes to copy, or 0 to copy all.
* Enter the file path of the destination file (must exist).
